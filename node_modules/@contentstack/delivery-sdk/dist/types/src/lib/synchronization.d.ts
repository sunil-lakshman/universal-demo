import { AxiosInstance } from '@contentstack/core';
import { SyncStack, SyncType } from './types';
export declare function synchronization(client: AxiosInstance, params?: SyncStack | SyncType, recursive?: boolean): Promise<any>;
