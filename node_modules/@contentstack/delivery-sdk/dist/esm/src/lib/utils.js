import { Region } from './types';
export function getHost(region = Region.US, host) {
    if (host)
        return host;
    let url = 'cdn.contentstack.io';
    if (region !== Region.US) {
        url = region.toString().toLowerCase() + '-cdn.contentstack.com';
    }
    return url;
}
//# sourceMappingURL=utils.js.map